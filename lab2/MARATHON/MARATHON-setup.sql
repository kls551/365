CREATE TABLE Marathon (
   Place int PRIMARY KEY,
   Times Time,
   Pace Time,
   GroupPlace int,
   Groups char(6),
   Age int,
   Sex char(1),
   BIBNumber int,
   FirstName char(30),
   LastName char(30),
   Town char(40),
   State char(2)
)
